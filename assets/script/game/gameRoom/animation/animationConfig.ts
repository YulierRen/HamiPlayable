import type { TweenEasing } from "cc";

export const animationConfig = {
    moveDuration: 0.18,
    scaleDuration: 0.14,
    combinedDuration: 0.18,
    easing: "quadOut" as TweenEasing,
    batchStagger: 0.03,
};
